// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  AdminType,
  AdminCreateInput,
  AdminUpdateInput,
  ApiCredentialType,
  ApiCredentialCreateInput,
  ApiCredentialUpdateInput,
  TransactionType,
  TransactionCreateInput,
  TransactionUpdateInput,
  TransactionEventType,
  TransactionEventCreateInput,
  TransactionEventUpdateInput,
  LedgerType,
  LedgerCreateInput,
  LedgerUpdateInput,
  PaymentGatewayType,
  PaymentGatewayCreateInput,
  PaymentGatewayUpdateInput,
  SettingType,
  SettingCreateInput,
  SettingUpdateInput,
  ApiLogType,
  ApiLogCreateInput,
  ApiLogUpdateInput,
  WebhookType,
  WebhookCreateInput,
  WebhookUpdateInput,
  NotificationType,
  NotificationCreateInput,
  NotificationUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Admin Client Functions
// ============================================================================

/**
 * List all Admin records
 */
export async function getAdminList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AdminType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/admins${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Admin')
  }
  const json = await response.json()
  return (json.items || []) as AdminType[]
}

/**
 * Get a single Admin by ID
 */
export async function getAdminById(args: { data: { id: string; userId?: string } }): Promise<AdminType> {
  const url = `${getApiBase()}/api/admins/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Admin not found')
  }
  const json = await response.json()
  return json.data as AdminType
}

/**
 * Create a new Admin
 */
export async function createAdmin(args: { data: { input: AdminCreateInput; userId?: string } }): Promise<AdminType> {
  const url = `${getApiBase()}/api/admins`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Admin')
  }
  const json = await response.json()
  return json.data as AdminType
}

/**
 * Update an existing Admin
 */
export async function updateAdmin(args: { data: { id: string; input: AdminUpdateInput; userId?: string } }): Promise<AdminType> {
  const url = `${getApiBase()}/api/admins/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Admin')
  }
  const json = await response.json()
  return json.data as AdminType
}

/**
 * Delete a Admin
 */
export async function deleteAdmin(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/admins/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Admin')
  }
  return { success: true }
}

// ============================================================================
// ApiCredential Client Functions
// ============================================================================

/**
 * List all ApiCredential records
 */
export async function getApiCredentialList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ApiCredentialType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/api-credentials${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ApiCredential')
  }
  const json = await response.json()
  return (json.items || []) as ApiCredentialType[]
}

/**
 * Get a single ApiCredential by ID
 */
export async function getApiCredentialById(args: { data: { id: string; userId?: string } }): Promise<ApiCredentialType> {
  const url = `${getApiBase()}/api/api-credentials/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ApiCredential not found')
  }
  const json = await response.json()
  return json.data as ApiCredentialType
}

/**
 * Create a new ApiCredential
 */
export async function createApiCredential(args: { data: { input: ApiCredentialCreateInput; userId?: string } }): Promise<ApiCredentialType> {
  const url = `${getApiBase()}/api/api-credentials`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ApiCredential')
  }
  const json = await response.json()
  return json.data as ApiCredentialType
}

/**
 * Update an existing ApiCredential
 */
export async function updateApiCredential(args: { data: { id: string; input: ApiCredentialUpdateInput; userId?: string } }): Promise<ApiCredentialType> {
  const url = `${getApiBase()}/api/api-credentials/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ApiCredential')
  }
  const json = await response.json()
  return json.data as ApiCredentialType
}

/**
 * Delete a ApiCredential
 */
export async function deleteApiCredential(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/api-credentials/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ApiCredential')
  }
  return { success: true }
}

// ============================================================================
// Transaction Client Functions
// ============================================================================

/**
 * List all Transaction records
 */
export async function getTransactionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TransactionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/transactions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Transaction')
  }
  const json = await response.json()
  return (json.items || []) as TransactionType[]
}

/**
 * Get a single Transaction by ID
 */
export async function getTransactionById(args: { data: { id: string; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Transaction not found')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Create a new Transaction
 */
export async function createTransaction(args: { data: { input: TransactionCreateInput; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Transaction')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Update an existing Transaction
 */
export async function updateTransaction(args: { data: { id: string; input: TransactionUpdateInput; userId?: string } }): Promise<TransactionType> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Transaction')
  }
  const json = await response.json()
  return json.data as TransactionType
}

/**
 * Delete a Transaction
 */
export async function deleteTransaction(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Transaction')
  }
  return { success: true }
}

// ============================================================================
// TransactionEvent Client Functions
// ============================================================================

/**
 * List all TransactionEvent records
 */
export async function getTransactionEventList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TransactionEventType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/transaction-events${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list TransactionEvent')
  }
  const json = await response.json()
  return (json.items || []) as TransactionEventType[]
}

/**
 * Get a single TransactionEvent by ID
 */
export async function getTransactionEventById(args: { data: { id: string; userId?: string } }): Promise<TransactionEventType> {
  const url = `${getApiBase()}/api/transaction-events/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'TransactionEvent not found')
  }
  const json = await response.json()
  return json.data as TransactionEventType
}

/**
 * Create a new TransactionEvent
 */
export async function createTransactionEvent(args: { data: { input: TransactionEventCreateInput; userId?: string } }): Promise<TransactionEventType> {
  const url = `${getApiBase()}/api/transaction-events`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create TransactionEvent')
  }
  const json = await response.json()
  return json.data as TransactionEventType
}

/**
 * Update an existing TransactionEvent
 */
export async function updateTransactionEvent(args: { data: { id: string; input: TransactionEventUpdateInput; userId?: string } }): Promise<TransactionEventType> {
  const url = `${getApiBase()}/api/transaction-events/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update TransactionEvent')
  }
  const json = await response.json()
  return json.data as TransactionEventType
}

/**
 * Delete a TransactionEvent
 */
export async function deleteTransactionEvent(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/transaction-events/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete TransactionEvent')
  }
  return { success: true }
}

// ============================================================================
// Ledger Client Functions
// ============================================================================

/**
 * List all Ledger records
 */
export async function getLedgerList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<LedgerType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/ledgers${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Ledger')
  }
  const json = await response.json()
  return (json.items || []) as LedgerType[]
}

/**
 * Get a single Ledger by ID
 */
export async function getLedgerById(args: { data: { id: string; userId?: string } }): Promise<LedgerType> {
  const url = `${getApiBase()}/api/ledgers/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Ledger not found')
  }
  const json = await response.json()
  return json.data as LedgerType
}

/**
 * Create a new Ledger
 */
export async function createLedger(args: { data: { input: LedgerCreateInput; userId?: string } }): Promise<LedgerType> {
  const url = `${getApiBase()}/api/ledgers`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Ledger')
  }
  const json = await response.json()
  return json.data as LedgerType
}

/**
 * Update an existing Ledger
 */
export async function updateLedger(args: { data: { id: string; input: LedgerUpdateInput; userId?: string } }): Promise<LedgerType> {
  const url = `${getApiBase()}/api/ledgers/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Ledger')
  }
  const json = await response.json()
  return json.data as LedgerType
}

/**
 * Delete a Ledger
 */
export async function deleteLedger(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/ledgers/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Ledger')
  }
  return { success: true }
}

// ============================================================================
// PaymentGateway Client Functions
// ============================================================================

/**
 * List all PaymentGateway records
 */
export async function getPaymentGatewayList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PaymentGatewayType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/payment-gatewaies${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list PaymentGateway')
  }
  const json = await response.json()
  return (json.items || []) as PaymentGatewayType[]
}

/**
 * Get a single PaymentGateway by ID
 */
export async function getPaymentGatewayById(args: { data: { id: string; userId?: string } }): Promise<PaymentGatewayType> {
  const url = `${getApiBase()}/api/payment-gatewaies/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'PaymentGateway not found')
  }
  const json = await response.json()
  return json.data as PaymentGatewayType
}

/**
 * Create a new PaymentGateway
 */
export async function createPaymentGateway(args: { data: { input: PaymentGatewayCreateInput; userId?: string } }): Promise<PaymentGatewayType> {
  const url = `${getApiBase()}/api/payment-gatewaies`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create PaymentGateway')
  }
  const json = await response.json()
  return json.data as PaymentGatewayType
}

/**
 * Update an existing PaymentGateway
 */
export async function updatePaymentGateway(args: { data: { id: string; input: PaymentGatewayUpdateInput; userId?: string } }): Promise<PaymentGatewayType> {
  const url = `${getApiBase()}/api/payment-gatewaies/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update PaymentGateway')
  }
  const json = await response.json()
  return json.data as PaymentGatewayType
}

/**
 * Delete a PaymentGateway
 */
export async function deletePaymentGateway(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/payment-gatewaies/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete PaymentGateway')
  }
  return { success: true }
}

// ============================================================================
// Setting Client Functions
// ============================================================================

/**
 * List all Setting records
 */
export async function getSettingList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SettingType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/settings${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Setting')
  }
  const json = await response.json()
  return (json.items || []) as SettingType[]
}

/**
 * Get a single Setting by ID
 */
export async function getSettingById(args: { data: { id: string; userId?: string } }): Promise<SettingType> {
  const url = `${getApiBase()}/api/settings/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Setting not found')
  }
  const json = await response.json()
  return json.data as SettingType
}

/**
 * Create a new Setting
 */
export async function createSetting(args: { data: { input: SettingCreateInput; userId?: string } }): Promise<SettingType> {
  const url = `${getApiBase()}/api/settings`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Setting')
  }
  const json = await response.json()
  return json.data as SettingType
}

/**
 * Update an existing Setting
 */
export async function updateSetting(args: { data: { id: string; input: SettingUpdateInput; userId?: string } }): Promise<SettingType> {
  const url = `${getApiBase()}/api/settings/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Setting')
  }
  const json = await response.json()
  return json.data as SettingType
}

/**
 * Delete a Setting
 */
export async function deleteSetting(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/settings/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Setting')
  }
  return { success: true }
}

// ============================================================================
// ApiLog Client Functions
// ============================================================================

/**
 * List all ApiLog records
 */
export async function getApiLogList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ApiLogType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/api-logs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ApiLog')
  }
  const json = await response.json()
  return (json.items || []) as ApiLogType[]
}

/**
 * Get a single ApiLog by ID
 */
export async function getApiLogById(args: { data: { id: string; userId?: string } }): Promise<ApiLogType> {
  const url = `${getApiBase()}/api/api-logs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ApiLog not found')
  }
  const json = await response.json()
  return json.data as ApiLogType
}

/**
 * Create a new ApiLog
 */
export async function createApiLog(args: { data: { input: ApiLogCreateInput; userId?: string } }): Promise<ApiLogType> {
  const url = `${getApiBase()}/api/api-logs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ApiLog')
  }
  const json = await response.json()
  return json.data as ApiLogType
}

/**
 * Update an existing ApiLog
 */
export async function updateApiLog(args: { data: { id: string; input: ApiLogUpdateInput; userId?: string } }): Promise<ApiLogType> {
  const url = `${getApiBase()}/api/api-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ApiLog')
  }
  const json = await response.json()
  return json.data as ApiLogType
}

/**
 * Delete a ApiLog
 */
export async function deleteApiLog(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/api-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ApiLog')
  }
  return { success: true }
}

// ============================================================================
// Webhook Client Functions
// ============================================================================

/**
 * List all Webhook records
 */
export async function getWebhookList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WebhookType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/webhooks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Webhook')
  }
  const json = await response.json()
  return (json.items || []) as WebhookType[]
}

/**
 * Get a single Webhook by ID
 */
export async function getWebhookById(args: { data: { id: string; userId?: string } }): Promise<WebhookType> {
  const url = `${getApiBase()}/api/webhooks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Webhook not found')
  }
  const json = await response.json()
  return json.data as WebhookType
}

/**
 * Create a new Webhook
 */
export async function createWebhook(args: { data: { input: WebhookCreateInput; userId?: string } }): Promise<WebhookType> {
  const url = `${getApiBase()}/api/webhooks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Webhook')
  }
  const json = await response.json()
  return json.data as WebhookType
}

/**
 * Update an existing Webhook
 */
export async function updateWebhook(args: { data: { id: string; input: WebhookUpdateInput; userId?: string } }): Promise<WebhookType> {
  const url = `${getApiBase()}/api/webhooks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Webhook')
  }
  const json = await response.json()
  return json.data as WebhookType
}

/**
 * Delete a Webhook
 */
export async function deleteWebhook(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/webhooks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Webhook')
  }
  return { success: true }
}

// ============================================================================
// Notification Client Functions
// ============================================================================

/**
 * List all Notification records
 */
export async function getNotificationList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<NotificationType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/notifications${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Notification')
  }
  const json = await response.json()
  return (json.items || []) as NotificationType[]
}

/**
 * Get a single Notification by ID
 */
export async function getNotificationById(args: { data: { id: string; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Notification not found')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Create a new Notification
 */
export async function createNotification(args: { data: { input: NotificationCreateInput; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Notification')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Update an existing Notification
 */
export async function updateNotification(args: { data: { id: string; input: NotificationUpdateInput; userId?: string } }): Promise<NotificationType> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Notification')
  }
  const json = await response.json()
  return json.data as NotificationType
}

/**
 * Delete a Notification
 */
export async function deleteNotification(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/notifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Notification')
  }
  return { success: true }
}
